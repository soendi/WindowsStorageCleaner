<Window x:Class="WindowsStorageCleaner.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bestätigung erforderlich"
        Width="560" MinHeight="350" MaxHeight="600"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Bestätigung erforderlich"
                   FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ErrorBrush}" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Background="{DynamicResource WarningBackgroundBrush}"
                BorderBrush="{DynamicResource WarningBorderBrush}" BorderThickness="1"
                CornerRadius="4" Padding="12" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="{Binding ItemName}" FontWeight="Bold" FontSize="14"
                           Foreground="{DynamicResource WarningForegroundBrush}" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding WarningText}" TextWrapping="Wrap"
                           Foreground="{DynamicResource WarningTextBrush}"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="Diese Aktion kann nicht rückgängig gemacht werden."
                   FontWeight="Bold" Foreground="{DynamicResource ErrorBrush}" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="3" Text="Geben Sie exakt JA ein, um fortzufahren:" Margin="0,0,0,8"/>

        <TextBox Grid.Row="4" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 Height="32" FontSize="14" TextAlignment="Center"
                 CharacterCasing="Upper" Margin="0,0,0,15"
                 />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Abbrechen" Command="{Binding CancelCommand}"
                    Width="100" Height="32" Margin="0,0,10,0"
                    Style="{DynamicResource ActionButtonStyle}"/>
            <Button Content="Weiter" Command="{Binding ConfirmCommand}"
                    Width="100" Height="32"
                    Style="{DynamicResource AccentButtonStyle}"
                    IsEnabled="{Binding CanConfirm}"/>
        </StackPanel>
    </Grid>
</Window>
