<Window x:Class="WindowsStorageCleaner.Views.ScheduledCleanupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Automatische Speicherplatzbereinigung"
        Width="540" MinHeight="350" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Automatische Bereinigung" FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource ErrorBrush}" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="1" Text="Richten Sie eine regelmäßige Speicherplatzbereinigung ein. Die Bereinigung läuft um 10:00 Uhr im Hintergrund."
                   TextWrapping="Wrap" Margin="0,0,0,12"/>

        <!-- Schedule list -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <ItemsControl x:Name="ScheduleList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                CornerRadius="4" Padding="10,8" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding FriendlyName}" FontWeight="Bold" FontSize="13"/>
                                    <TextBlock Text="{Binding NextRun, StringFormat='Nächste Ausführung: {0}'}"
                                               FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="{Binding ToggleLabel}" Width="80" Height="26" FontSize="11"
                                            Style="{DynamicResource ActionButtonStyle}"
                                            Click="OnToggleTask" Margin="0,0,4,0"/>
                                    <Button Content="Löschen" Width="70" Height="26" FontSize="11"
                                            Style="{DynamicResource WarningButtonStyle}"
                                            Click="OnDeleteTask"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- New schedule form -->
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,6,0">
                <TextBlock Text="Häufigkeit:" FontSize="11" Margin="0,0,0,2"/>
                <ComboBox x:Name="FrequencyCombo" Height="28" FontSize="12"
                          Style="{DynamicResource ComboBoxStyle}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="0,0,6,0">
                <TextBlock Text="Profil:" FontSize="11" Margin="0,0,0,2"/>
                <ComboBox x:Name="ProfileCombo" Height="28" FontSize="12"
                          Style="{DynamicResource ComboBoxStyle}"/>
            </StackPanel>
            <Button Grid.Column="2" Content="Hinzufügen" Width="90" Height="28" FontSize="11"
                    Style="{DynamicResource AccentButtonStyle}" Click="OnAddScheduleClick"
                    VerticalAlignment="Bottom"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Schließen" Width="100" Height="32"
                    Style="{DynamicResource ActionButtonStyle}" Click="OnCloseClick"/>
        </StackPanel>
    </Grid>
</Window>
